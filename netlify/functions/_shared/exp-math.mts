/**
 * Thin wrapper — canonical implementation lives in shared/lib/exp-math.ts.
 * Re-exported here so existing function-layer imports continue to resolve.
 */
export { expScaled, EXP_SCALE, LN_100_SCALED } from "../../../shared/lib/exp-math.ts";
